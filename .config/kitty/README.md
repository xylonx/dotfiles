# Kitty configuration

- `kitty-scrollback.nvim.conf` requires commands generation. See its conents for more information
- The font is `Space Mono for Powerline` which could be found at `https://github.com/powerline/fonts/tree/master/SpaceMono`

